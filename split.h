#include <iostream>
#include <string>
#include <vector>

/*
 * Функция, разделяющая строку на подстроки по указанному разделителю.
 */
std::vector<std::string> Split(const std::string& line, char delimiter);
